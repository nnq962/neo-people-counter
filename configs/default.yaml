model:
  path: models/yolov8n.pt
  confidence: 0.5
  device: cpu          # cpu | cuda | mps

video:
  source: 0            # 0 = webcam, hoặc đường dẫn file video
  width: 1280
  height: 720

counter:
  mode: line           # line | zone
  line_position: 0.5   # 0.0 - 1.0 (tỉ lệ chiều cao frame)
